git : 

    git config --global user.name "Aseel Biadsy"
    git config --global user.email asilbia@post.bgu.ac.il
    git config --global -e
    git init            (initialize git repository)
    git config --global core.editor "code --wait"

    git log --online
    creating a commit its like taking a snapshot of the project

    git status 
    git add file1.txt
    git add file2.txt
    git commit -m "this is my first git commit not using the GUI"

    git rm file2.txt
    git mv file3.txt
    touch .gitignore

    git branch aseel    (creat beanch with name aseel)
    git branch (List all branches)
    git branch -d aseel  (delete the branch)
    git branch -M new-name (this renames the current branch t a new name)

    git checkout branch-name (Switches to that branch)
    git checkout file   (Restores file to last commit)

   

    git diff  -- staged  (sateged vs last commit)
    git diff    (unstaged vs last commit)
    git diff  commitPid1   commitPid2

    
    git stash (to save the current directory and it return to the last commit )
    git stash pop (get back to the original version after we where before we used the stash)



    git checkout  "commit id" moves to the head to the specif commit
    git checkout master   (moves you to the latest version of the code )
    git reflog  (moves you back where you were previously)
  

    git rebase I skipped it .........
    
    






            



    GitHub : 

    
        git remote add origin (name)  https://github.com/Aseel205/image.git  (url)  
        git remote rename oldname new name
        git remote remove name (to remove the connrection wuth the remote)
        git remote -v (shows you the fetch and the push)
        
        git push -u origin main  (here you are making a link between the master and the origin)
        git push origin master 


        git clone  <URL> you dowload the code into your computer..

        git fetch : get into but dont put  in my work
        git pull origin mainok  :  get info and add it in my work 
        gti pull = git fetch + git merge (changes will be merge to main)

        git restore. (restore the deleted files)